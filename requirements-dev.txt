# ============================================================
# DocMind — 开发与 CI 依赖
# ============================================================
# 安装: pip install -r requirements-dev.txt
# 用途: 本地开发、代码检查、单元测试、CI/CD
# ============================================================

# 测试
pytest>=8.0
pytest-cov>=5.0
pytest-mock>=3.12

# 代码质量
ruff>=0.5
mypy>=1.10

# 类型存根（mypy 需要）
types-requests>=2.31
